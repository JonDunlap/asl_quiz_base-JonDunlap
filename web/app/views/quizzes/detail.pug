extends ../_layout
include ../_includes/link_mixin
block content
	h1.heading= quiz.name
		+link('/admin/quizzes/'+quiz.id, 'View')
		+link('/admin/quizzes/edit/'+quiz.id, 'Edit', 'fa-edit')
		+link('/admin/quizzes/delete/'+quiz.id, 'Delete', 'fa-trash', 'linkSecondary')
	h2.headingSecondary Questions
	ul.list 
		if questions
			for question in questions
				li.list__item
					span.list__item__title= question.title
					+link('/admin/questions/'+question.id, 'View')
					+link('/admin/questions/delete/'+question.id, 'Delete', 'fa-trash', 'linkSecondary')
	a.button.primary(href="/admin/questions/new?quizId="+quiz.id) Add a new question